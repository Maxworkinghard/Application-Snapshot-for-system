<Window x:Class="WindowSnap.Wpf.Settings.ShortcutSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="应用快照快捷键"
        Width="360" Height="200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="快捷键" FontSize="20" FontWeight="SemiBold" />

        <TextBlock Grid.Row="1" Text="按下你想使用的组合键"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Margin="0,6,0,0" />

        <Border Grid.Row="2" Margin="0,20,0,0" Height="48"
                CornerRadius="8"
                Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                BorderThickness="1"
                Focusable="True"
                x:Name="Recorder"
                KeyDown="OnRecorderKeyDown"
                GotFocus="OnRecorderGotFocus"
                LostFocus="OnRecorderLostFocus">
            <TextBlock x:Name="RecorderLabel" VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="17" FontWeight="Medium" />
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Padding="16,4" Margin="0,0,8,0"
                    Click="OnCancelClick" />
            <Button x:Name="SaveButton" Content="保存" Padding="16,4"
                    IsDefault="True"
                    Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</Window>
