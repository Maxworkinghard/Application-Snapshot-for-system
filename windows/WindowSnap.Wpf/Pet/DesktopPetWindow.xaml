<Window x:Class="WindowSnap.Wpf.Pet.DesktopPetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="应用快照宠物"
        Width="64" Height="64"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        ShowActivated="False">
    <Border x:Name="PetBorder"
            Width="56" Height="56"
            CornerRadius="28"
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
            BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            BorderThickness="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3" Color="Black" />
        </Border.Effect>
        <Grid>
            <Image x:Name="IconImage" Stretch="Uniform" Margin="6" />
            <Popup x:Name="ActionPopup"
                   Placement="Top"
                   PopupAnimation="Fade"
                   StaysOpen="False">
                <Border Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
                        BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel>
                        <TextBlock x:Name="TargetName" FontWeight="SemiBold" FontSize="13" HorizontalAlignment="Center" />
                        <Button x:Name="CaptureButton" Margin="0,10,0,0" Padding="16,4" Click="OnCaptureClick">
                            截取窗口
                        </Button>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>
    </Border>
</Window>
